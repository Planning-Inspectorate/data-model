{
	"$schema": "http://json-schema.org/draft-07/schema#",
	"$id": "appeal-statement.schema.json",
	"title": "Appeal Statement",
	"description": "Schema defining statement made by a party on an appeal",
	"type": "object",
	"additionalProperties": true,
    "required": ["caseReference", "submittedDate"],
	"properties": {
        "caseReference": {
            "type": "string",
            "examples": ["7002471"],
            "description": "External case identifier"
        },
        "serviceUserId": {
            "type": ["string", "null"],
            "description": "Maps to [ServiceUser].[id], will be rule 6, agent or appellant"
        },
        "lpaCode": {
            "type": ["string", "null"],
            "examples": ["Q9999"],
            "description": "A unique identifier for the Local Planning Authority",
            "$comment": "Uses the internal PINS code identifying LPAs"          
        },
        "statement": {
            "type": ["string", "null"],
            "examples": ["I have concerns"]
        },
        "submittedDate": {
            "type": ["string"],
            "format": "date-time",
            "examples": ["2023-07-27T20:30:00.000Z"],
            "description": "The date the statement was submitted by the user"
        },
        "documentIds": {
            "type": "array",
            "items": {
                "type": "string",
                "examples": ["4547fec8-16b2-47bb-836d-4d0baac04079"],
                "description": "The unique identifier for the document, maps to [AppealDocument].[id]"
			}
        }
    }
}