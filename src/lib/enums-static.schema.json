{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "enums-static.schema.json",
  "title": "Static Enums",
  "description": "Static enums to add to generated types that are not directly references in schemas",
  "type": "object",
  "properties": {
    "eventType": {
      "type": "string",
      "enum": ["Create", "Update", "Delete", "Publish", "Unpublish"],
      "description": "The type of Service Bus event"
    }
  }
}
