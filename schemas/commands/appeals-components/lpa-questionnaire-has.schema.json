{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "commands/appeals-components/lpa-questionnaire-has.schema.json",
  "title": "LPAQ HAS submission properties",
  "description": "Schema defining any HAS specific properties for LPAQ submissions",
  "type": "object",
  "additionalProperties": true,
  "required": [
    "affectedListedBuildingNumbers",
    "isGreenBelt",
    "inConservationArea",
    "newConditionDetails",
    "notificationMethod",
    "lpaCostsAppliedFor",
    "lpaStatement"
  ],
  "allOf": [
    {
      "properties": {
        "affectedListedBuildingNumbers": {
          "type": ["array", "null"],
          "items": {
            "type": "string",
            "examples": ["10023", "17824"]
          },
          "description": "A list of affected listed building IDs from Historic England"
        },
        "isGreenBelt": {
          "type": ["boolean", "null"],
          "description": "Indicates if the site is in a green belt"
        },

        "inConservationArea": {
          "type": ["boolean", "null"],
          "description": "Indicates if the site is in a conservation area"
        },
        "newConditionDetails": {
          "type": ["string", "null"],
          "examples": ["Since the original application, a number of things have changed..."],
          "description": "New conditions details provided by the LPA"
        },
        "notificationMethod": {
          "type": ["array", "null"],
          "items": {
            "type": "string",
            "examples": ["notice", "letter", "advert"]
          },
          "description": "The methods used to notify relevant parties"
        },

        "lpaCostsAppliedFor": {
          "type": ["boolean", "null"],
          "description": "Indicates if the LPA has applied for costs"
        },
        "lpaStatement": {
          "type": ["string", "null"],
          "examples": ["We refused the application because of ..."],
          "description": "A statement provided by the LPA, unused?"
        },
        "listOfDocumentsBeforeDecision": {
          "type": ["string", "null"],
          "description": "Free text response from LPA listing documents used to drive their decision"
        },
        "significantChangesAffectingApplicationLpa": {
          "type": ["array", "null"],
          "items": {
            "type": "object",
            "properties": {
              "value": {
                "type": ["string", "null"],
                "enum": ["adopted-a-new-local-plan", "national-policy-change", "court-judgement", "other", null]
              },
              "comment": {
                "type": ["string", "null"]
              }
            },
            "required": ["value"]
          },
          "description": "Significant changes affecting the application as reported by the LPA"
        }
      }
    }
  ]
}
